machine:
  node:
    version: 6.9.0

dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - meteor || curl https://install.meteor.com | /bin/sh

test:
  override:
    - npm test
    - npm test:server
